describe('<%= scriptAppName %>', function() {
    'use strict';

    beforeEach(module('<%= scriptAppName %>'));

    it('should always work', function() {
        expect(true)
            .toBe(true);
    });
});
