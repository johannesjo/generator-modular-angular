describe('<%= scriptAppName %>', () => {
    'use strict';

    beforeEach(module('<%= scriptAppName %>'));

    it('should always work', () => {
        expect(true)
            .toBe(true);
    });
});
