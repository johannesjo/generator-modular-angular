describe('<%= scriptAppName %>.routes', () => {
    'use strict';

    beforeEach(module('<%= scriptAppName %>'));

    it('should be defined', () => {
        expect(true)
            .toBe(true);
    });
});