describe('<%= scriptAppName %>.routes', function() {
    'use strict';

    beforeEach(module('<%= scriptAppName %>'));

    it('should be defined', function() {
        expect(true)
            .toBe(true);
    });
});